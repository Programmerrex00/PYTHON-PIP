import main

# Me compila main si lo importo automaticamente, pero si meto el codigo que tiene main en una funcion que en este caso es run() puedo controlar lo que quiero ejecutar
main.run()
print('Esto viene del archivo example: ', main.data)
